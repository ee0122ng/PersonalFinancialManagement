export interface RegisterForm {
    username: string;
    password: string;
    email: string;
}

export interface Country {
    name: string;
    flag: string;
}

export interface AccountCredential {
    username: string;
    password: string;
}